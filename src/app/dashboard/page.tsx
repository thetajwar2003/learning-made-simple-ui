'use client';
import Header from '@/components/Header';

export default function Dashboard() {
  // TODO: redirect to / if user not logged in

  /**
   * find type of user,
   * produce appropriate content after header
   */

  return (
    <>
      <Header />
    </>
  );
}
